// NativeWind v4 PostCSS configuration
module.exports = {
  plugins: {
    tailwindcss: {},
    // NativeWind v4 doesn't require postcss plugin
    // Styles are processed at build time via metro.config.js
  }
}; 